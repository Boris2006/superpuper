#include "header.h"
int main()
{
    string a, b, m;
    int y;
    long long e;
    string c, d;
    cin >> a;
    b = oprst(a);
    c = first(a);
    d = second(a);
    e = len(a);
    m = o(c, d);
    cout << m;
    return 0;
}
